name: shared
description: Shared models, services, widgets, and utilities for WTF Flutter apps.
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  firebase_core: ^2.24.0
  cloud_firestore: ^4.14.0
  equatable: ^2.0.5
  intl: ^0.18.1
  uuid: ^4.2.1
  logger: ^2.0.2
  provider: ^6.1.2

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter
