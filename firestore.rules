rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TODO: Replace open dev rules with role-scoped access before production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
